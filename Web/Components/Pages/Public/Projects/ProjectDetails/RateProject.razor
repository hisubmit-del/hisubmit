@inject IStringLocalizer<RateProject> Localize

@if (_loaded && RefereePermission.Allowed)
{
    @if (RefereePermission.Judgings.Count > 1)
    {
        <MudSelect T="int" Dense="true" Variant="Variant.Outlined"
                   Label="@Localize["Assigned festival"]"
                   Value="@_selectedJudgingId"
                   ValueChanged="SelectJudging"
                   Class="mb-2">
            @foreach (var assignment in RefereePermission.Judgings)
            {
                <MudSelectItem T="int" Value="@assignment.Id">@assignment.FestivalName</MudSelectItem>
            }
        </MudSelect>
    }
    <MudChip T="string" Color="Color.Tertiary" Variant="Variant.Text">TN:@_judgingDetail.Submit.TrackingCode</MudChip>
    
    <MudButton Class="mx-1" Variant="Variant.Filled"
               StartIcon="@Icons.Material.Filled.RateReview"
               OnClick="ShowRefereeModal"
               Color="Color.Tertiary">Rate this Project</MudButton>
    
    <MudButton Class="mx-1" Variant="Variant.Outlined" 
               StartIcon="@Icons.Material.Filled.AlignVerticalBottom"
               Color="Color.Dark">@_judgingDetail.Submit.SubmitStatus</MudButton>
    
    <MudButton Class="mx-1" Variant="Variant.Outlined"
               StartIcon="@Icons.Material.Filled.AlignVerticalBottom"
               Color="Color.Dark">@(_judgingDetail.JudgingButton==null ? "Judging Enable ":_judgingDetail.JudgingButton.Name)</MudButton>
    
    
    @* <div class="detail-box mb-4"> *@
    @*     <h4>@Localize["Judging Enable"]</h4> *@
    @*     <div class="ma-2"> *@
    @*         @if (_judgingDetail.JudgingButton != null) *@
    @*         { *@
    @*             <MudText Typo="Typo.body2"> *@
    @*                 @_judgingDetail.JudgingButton.Name *@
    @*             </MudText>   *@
    @*         } *@
    @*     </div> *@
    @* </div> *@
    @* *@
    @* <MudButton Color="Color.Primary" *@
    @*            Variant="Variant.Filled" *@
    @*            OnClick="ShowRefereeModal"> *@
    @*    @Localize[" Rate Project"] *@
    @* </MudButton> *@
}
