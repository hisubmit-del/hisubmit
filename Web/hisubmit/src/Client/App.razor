@using System.Reflection
@using Microsoft.AspNetCore.Components.WebAssembly.Services
<CascadingAuthenticationState>
<Error>
    <Router
        AdditionalAssemblies="_lazyLoadedAssemblies"
        OnNavigateAsync="OnNavigateAsync"
        AppAssembly="@typeof(App).Assembly">
        <Found Context="routeData">
            <AuthorizeRouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)">
                <NotAuthorized>
                    <RedirectToLogin/>
                </NotAuthorized>
            </AuthorizeRouteView>
        </Found>
        <NotFound>
           
                <LayoutView Layout="@typeof(NotFoundLayout)">
                    <p>Sorry, there's nothing at this address.</p>
                </LayoutView>
           
        </NotFound>
    </Router>
</Error>
</CascadingAuthenticationState>

@* @inject LazyAssemblyLoader AssemblyLoader *@

@code{
    private readonly List<Assembly> _lazyLoadedAssemblies = [];

    private async Task OnNavigateAsync
       (Microsoft.AspNetCore.Components.Routing.NavigationContext args)
    {
        if (args.Path.Contains("project"))
        {
            // var assemblies = await AssemblyLoader.LoadAssembliesAsync(
            //     new[]
            //     {
            //         //"BlazorVideoPlayer.wasm",
            //         // "Blazored.Video.wasm",
            //         // "Blazorise.Video.wasm"
            //     });
            // _lazyLoadedAssemblies.AddRange(assemblies);
        }
    }
}
