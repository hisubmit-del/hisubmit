@page "/admin/news"
@page "/admin/news/{FestivalId:int}"

@using global::Hisubmit.Client.SharedModels.Features.News.Queries
@inject IStringLocalizer<News> Localize
@inherits BaseAdminPages
<style>
    .mud-table-container {
        overflow: auto;
    }
</style>

<MudContainer MaxWidth="MaxWidth.Medium">
    <HeroTitle Title="@Localize["News"]" Description="@Localize["News and Articles"]" />

    <MudTable Loading="!_loaded" LoadingProgressColor="Color.Secondary" class="default-light-table" Elevation="25" Items="_pagedDate" TotalItems="_totalItems"
              ServerData="@(new Func<TableState,CancellationToken, Task<TableData<GetAllNewResponse>>>(ServerReload))" Hover="true" Dense="true" Bordered="false" Striped="true" @ref="_table">
        <ToolBarContent>
            <div class="justify-center mud-text-align-center">
                <MudHidden Breakpoint="Breakpoint.LgAndUp">
                    <MudIconButton DisableElevation Variant="Variant.Filled" OnClick="@(() => OnSearch(""))" Icon="@Icons.Material.Filled.Refresh" Color="Color.Secondary" />
                    @if (FestivalId == null)
                    {
                        <AuthorizeView Context="authorizeContext" Policy="@Permissions.Contents.UpdateNew">
                            <Authorized>
                                <MudIconButton Variant="Variant.Filled" Color="Color.Primary" OnClick="Add" Icon="@Icons.Material.Filled.Add" />
                            </Authorized>
                        </AuthorizeView>
                    }
                </MudHidden>
                <MudHidden Breakpoint="Breakpoint.MdAndDown">
                    <MudButtonGroup Color="Color.Primary" Variant="Variant.Filled" OverrideStyles="false">
                        <MudButton Variant="Variant.Filled" OnClick="@(() => OnSearch(""))" StartIcon="@Icons.Material.Filled.Refresh" IconColor="Color.Surface" Color="Color.Secondary">@Localize["Reload"]</MudButton>
                        @if (FestivalId == null)
                        {
                            <AuthorizeView Context="authorizeContext" Policy="@Permissions.Contents.UpdateNew">
                                <Authorized>
                                    <MudButton Variant="Variant.Filled" Color="Color.Primary" OnClick="Add" StartIcon="@Icons.Material.Filled.Add">Add New</MudButton>
                                </Authorized>
                            </AuthorizeView>
                        }
                    </MudButtonGroup>
                </MudHidden>
            </div>
            <MudSpacer />
            <MudTextField T="string" ValueChanged="@(OnSearch)" Placeholder="@Localize["Search"]"
                          Adornment="Adornment.Start" AdornmentIcon="@Icons.Material.Filled.Search"
                          IconSize="Size.Medium" Class="mt-0">
            </MudTextField>
        </ToolBarContent>
        <HeaderContent>
            <MudTh>@Localize["Banner"]</MudTh>
            <MudTh>@Localize["Title"]</MudTh>
            <MudTh>@Localize["Show in site"]</MudTh>
            <MudTh Style="text-align:center">@Localize["Actions"]</MudTh>
        </HeaderContent>
        <RowTemplate>
            <MudTd DataLabel="Banner">
                <MudImage Src="@context.BannerUrl" Width="100" Height="60" Alt="@context.Title" Elevation="25" Class="rounded-lg" />
            </MudTd>
            <MudTd DataLabel="Title">
                <MudHighlighter Text="@context.Title" HighlightedText="@_searchString" />
            </MudTd>
            <MudTd DataLabel="Show in site" Class="text-center">
                <span>
                    <MudSwitch T="bool" Value="@context.IsEnable"
                               Class="m-auto"
                               ThumbIcon="@(context.IsEnable == true ? Icons.Material.Filled.Done : Icons.Material.Filled.Close)"
                               ThumbIconColor="@(context.IsEnable == true ? Color.Tertiary : Color.Error)"
                               Color="Color.Success" T="bool" ValueChanged="(t) => ChangeChecked(context.Id, t)">
                    </MudSwitch>
                </span>
            </MudTd>
            <MudTd DataLabel="Actions">
                @if (FestivalId == null)
                {
                    <AuthorizeView Context="authorizeContext" Policy="@Permissions.Contents.UpdateNew">
                        <Authorized>
                            <MudHidden Breakpoint="Breakpoint.MdAndDown">
                                <MudIconButton Icon="@Icons.Material.Filled.Edit" Size="Size.Small" Title="Edit" OnClick="() => Edit(context.Id)" Color="Color.Default"></MudIconButton>
                                <MudIconButton Icon="@Icons.Material.Filled.Delete" Size="Size.Small" Title="Delete" OnClick="() => Delete(context.Id, context.Title)" Color="Color.Default"></MudIconButton>
                            </MudHidden>
                            <MudHidden Breakpoint="Breakpoint.LgAndUp">
                                <MudMenu Label="@Localize["Actions"]" Variant="Variant.Filled" DisableElevation="true"
                                         EndIcon="@Icons.Material.Filled.KeyboardArrowDown" IconColor="Color.Secondary" Direction="Direction.Left" OffsetX="true">
                                    <MudMenuItem OnTouch="() => Edit(context.Id)" OnClick="() => Edit(context.Id)">@Localize["Edit"]</MudMenuItem>
                                    <MudMenuItem OnTouch="() => Delete(context.Id, context.Title)" OnClick="() => Delete(context.Id, context.Title)">@Localize["Delete"]</MudMenuItem>
                                </MudMenu>
                            </MudHidden>
                        </Authorized>
                    </AuthorizeView>
                }
            </MudTd>
        </RowTemplate>
        <PagerContent>
            <TablePager />
        </PagerContent>
    </MudTable>
</MudContainer>