@layout MainLayout
@attribute [AllowAnonymous]
@inject IStringLocalizer<Login> Localize
@inherits HiSubmit.Web.Components.Pages.Public.BasePublicPages

<MudGrid Class="mt-lg-8" Justify="Justify.Center">
    <MudItem xs="12" md="6" lg="5" Class="pl-0">
        <MudPaper Class="pa-5">
            <EditForm Model="_tokenModel" OnValidSubmit="SubmitAsync">
                <FluentValidationValidator @ref="_fluentValidationValidator"/>
                <MudGrid Class=" pa-lg-8 pa-2" >
                    <DataAnnotationsValidator/>
                    <MudItem xs="12">
                        <div class="d-flex justify-center">
                            <MudText Color="Color.Primary" Typo="Typo.h4">@Localize["Title"]</MudText>
                        </div>
                    </MudItem>
                    <MudItem xs="12" Class="text-center">
                        <MudImage Style="width: 60%" Src="@ClientAppConstants.LogoUrl"></MudImage>
                    </MudItem>
                    <MudItem xs="12">
                        <div class="d-flex justify-center">
                            <MudText>
                                @Localize["Don't have an account?"] <MudLink Href="/register">@Localize["Register here"]</MudLink>
                            </MudText>
                        </div>
                    </MudItem>
                    <MudItem xs="12">
                        <MudTextField T="string" Label="@Localize["E-mail"]" 
                                      Required="true"Variant="Variant.Outlined"
                                      @bind-Value="_tokenModel.Email" For="@(() => _tokenModel.Email)"/>
                    </MudItem>
                    <MudItem xs="12">
                        <MudTextField Label="@Localize["Password"]"
                                      @bind-Value="_tokenModel.Password" Variant="Variant.Outlined" 
                                      For="@(() => _tokenModel.Password)"
                                      Required="true"
                                      InputType="@_passwordInput" Adornment="Adornment.End"
                                      AdornmentIcon="@_passwordInputIcon"
                                      OnAdornmentClick="TogglePasswordVisibility"/>
                    </MudItem>
                    <MudItem xs="12" Class="d-flex justify-space-between align-center">
                        <MudCheckBox T="bool" Label="@Localize["Remember me?"]" Color="Color.Primary" Class="ml-n1"></MudCheckBox>
                    </MudItem>
                    <MudItem xs="12" Class="d-flex justify-center">
                        <LoadingButton Processing="_processing" ButtonType="ButtonType.Submit" 
                                       Variant="Variant.Filled" Disabled="@(!_validated)"
                                       Color="Color.Dark" Size="Size.Large"
                                       Style="width: 100%;">@Localize["Sign In"]</LoadingButton>
                    </MudItem>
                    <MudItem xs="12" Class="text-center">
                        <MudLink   Typo="Typo.body1" Href="/account/forgot-password">@Localize["Forgot password?"]</MudLink>
                    </MudItem>
                </MudGrid>
            </EditForm>
        </MudPaper>
    </MudItem>
</MudGrid>
